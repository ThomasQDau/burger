<h1>Burgers</h1>

<h2>Burgers to be devoured</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      Burger #{{id}} {{burger_name}} <button class="devour" data-id="{{id}}">Devour!</button> <br>
    {{/unless}}
  {{/each}}
</ul>

<h2>Devoured burgers</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      Burger # {{id}} {{burger_name}} <br>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>
  <button type="submit">Add Burger</button>
</form>
